# 帳票別 金額・宛先の実装状況（現状のコード実測ベース）

各帳票について、金額部分・Ship to・Bill to 等が **現在コード上どのデータを引いているか（実測）** と、**修正/実装が必要な点** を整理。
コードから意図が読み取れない箇所は「**判断がつかない**」と明記（憶測なし）。出典は `a_filestatus/`。

凡例：✅=現状で成立 ／ ⚠=要修正・要実装 ／ ❓=判断がつかない（業務仕様の確認が必要）

---

## ■ 見積書（CB宛・pdf_cb.html｜英語）
- **金額部分**：✅ 見積り送付の明細＋小計＋TOTAL（運営手数料なし＝素の金額）。`pdf_cb.html:132-160`
- **Bill to**：Cosmo Bio Co Ltd（会社名・住所を**ハードコード**）。`pdf_cb.html:77-80` — 表示は成立。DB化すべきかは ❓
- **Ship to**：`[PDF_ADDRESS4]`。`index.php:617-635` の分岐＝**サプライヤーが日本以外→CB物流センター住所／日本→研究者の住所英語表記（M2_DVAL20）**。表示は成立。分岐条件が業務的に正しいかは ❓
- **相手情報**：Researcher information＝研究者会社名 `[PDF_M2-M2_ETC20]`＋担当者、右列にサプライヤー `[PDF_M1-M1_DVAL01]`＋住所 `[PDF_ADDRESS3]`。`pdf_cb.html:89-96` ✅
- **その他**：⚠(軽) 未使用hidden input `ETC23〜59` 残置（`pdf_cb.html:121-130`）、`[S-PDF_D_ITEMS]` は「用途不明」コメント（`:162`）。クリーンアップは任意

## ■ 見積書（研究者宛・pdf_r.html｜日本語）
- **金額部分**：✅ 明細＋手数料一式（PF手数料・輸入/輸出代行・運営値引）＋合計金額（運営手数料あり）。`pdf_r.html:80-149`
- **宛先**：3列併記＝研究者会社名 `[PDF_M2-M2_DVAL03]`／サプライヤー `[PDF_M1-M1_DVAL01]`＋`[PDF_ADDRESS3]`／コスモ・バイオ株式会社。宛名＝`[PDF_M2-M2_DVAL16][DVAL17] 様`。`pdf_r.html:41-61` ✅（「Bill to/Ship to」ラベルは無く3者併記の様式）
- **発行者**：コスモ・バイオ株式会社＋住所を**ハードコード**（`pdf_r.html:47,57`）。表示は成立。DB化は ❓

---

## ■ 発注書（CB→サプライヤー・hcb｜pdf_h.html）
- **金額部分**：⚠ **合計欄が無い**。明細の単価・行合計は出る（`pdf_h.html:70-100`、研究者面と同じ数値）が、手数料込み合計は `[PDF_H_TO_R]` 内＝hcbでは非表示、**手数料抜きの合計はどこにも組版されていない**。→ CB宛見積ベース（運営手数料なし）の合計を算出・表示する実装が必要。金額計算 `index.php:1499-2214` は preview_type 分岐なし
- **Ship to**：現状＝サプライヤー `[PDF_M1-M1_ETC128]`（会社名）＋`[PDF_ADDRESS1]`（サプライヤーのBill to住所1）＋`[PDF_M1-M1_ETC32]`（担当者）。`pdf_h.html:64` — 「送り先＝サプライヤー」が発注書仕様として正しいかは ❓
- **Bill to**：現状＝`[M2_SHIP_TO_SPT_2]`＋`[PDF_ADDRESS2]`。`pdf_h.html:61-62`
  - `[M2_SHIP_TO_SPT_2]` は**コード自身が「変数名の命名に誤り」と明記**、`pdf_cb` では「BILL TO(サプライヤー会社名)」と説明。`[PDF_ADDRESS2]`＝見積り送付時に選択されたBill to住所（`index.php:600-605`, FieldValue[66-70]）
  - → CB→サプライヤー発注書のBill toが**誰であるべきか（CBか等）**は ❓。現状値はサプライヤー由来のスナップショットで、意図と合致するか不明。**要確認**
- **宛先の描き分け**：⚠ この住所ブロック（`pdf_h.html:44-68`）は **hr/hcb 共通**（`[PDF_H_TO_*]` の外）＝**hcbとhrで宛先が切り替わらない**。CB/研究者/サプライヤーの出し分けは未実装
- **タイトル**：⚠ 無し。`<h1>` が無く `[PDF_H_TO_CB]` はテストスタブ（`pdf_h.html:2-7`）。実装が必要
- **発注日**：✅ `[HATYU_NEWDATE]`＝決済者発注承認 or 発注依頼の日（`index.php:715-733`）
- **番号**：現状 Quote No. を流用（`pdf_h.html:24`）。PO番号にすべきかは ❓

## ■ 発注書（研究者→CB・hr｜pdf_h.html）
- **金額部分**：✅ 見積り送付の明細＋手数料一式＋合計金額（運営手数料あり＝研究者宛見積ベース）。`pdf_h.html:104-188`
- **Ship to / Bill to**：⚠ **hcbと同一の住所ブロックを共有**（上記のとおり切り替わらない）。研究者→CB発注書なのに Ship to=サプライヤー、Bill to=スナップショットのまま。研究者宛の宛先への描き分けが必要
  - 研究者→CB発注書の Bill to / Ship to に**何を入れるべきか**は ❓（業務仕様が必要）
- **タイトル**：⚠ 無し（hcbと同じ）

---

## ■ 請求書（研究者宛・pdf_s.html）
- **金額部分**：⚠ **組版されない**。pdf_s に金額欄が無く、総額はアップロードPDF（`S2_FILE`）の中のみ。`pdf_s.html:1-67`
  - 載せるべき金額（研究者宛見積の合計と想定されるが、pdf_s は金額を保持しないため）**定義から要確定**
- **Ship to / Bill to**：⚠ **宛先ブロックが存在しない**。案件名／サプライヤー会社名 `[PDF_M1-M1_ETC128]`／研究者会社名 `[PDF_M2-M2_ETC20]`／日付／支払期限を並べる表のみ。`pdf_s.html:9-28`
  - 正式な宛先（御中/Bill to 等）が必要かは ❓
- **支払期限**：⚠ `[PDF_M3-M2_ETC03]`＝**決裁者マスタ DAT_M3 の静的値**（`index.php:549-563`）。取引ごとの値にすべきかは要確定
- **発行者**：ロゴのみ（コスモバイオ）。発行者住所ブロックは無し

## ■ 追加請求書（pdf_s_add.html）
- **金額部分**：✅（表示のみ）追加請求金額 `[PDF_S_ADD_CHARGE2]`＝FieldValue[90] を組版。`pdf_s_add.html:34-35`。Invoice参照は元の請求（研究者）の `S2_FILE` を流用
- **Ship to / Bill to**：⚠ pdf_s 同様に**宛先ブロックなし**（当事者名の列挙のみ）
- **支払期限**：⚠ 同じく DAT_M3.M2_ETC03

---

## 「判断がつかない（要・業務仕様確認）」一覧
1. 発注書（CB→サプライヤー）の **Bill to は誰か**（現状はサプライヤー由来スナップショット、命名誤りあり）
2. 発注書（CB→サプライヤー）の **Ship to＝サプライヤー** で正しいか
3. 発注書（研究者→CB）の **Bill to / Ship to に何を入れるか**
4. 見積書（CB宛）の Ship to 分岐（日本/海外でCB物流センター↔研究者住所）の妥当性
5. 請求書（研究者宛）に **組版すべき金額の定義**、および **正式な宛先表記の要否**
6. 支払期限を決裁者マスタ値のままにするか、取引ごとの値にするか
7. CB自社住所・宛先のハードコードを DB 化するか
8. 発注書の番号を Quote No. 流用のままにするか、PO番号を採るか
